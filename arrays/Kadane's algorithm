class Solution:
    def maxSubarraySum(self, arr):
        max_sum=float('-inf')
        curr_sum=0
        for i in range(0,len(arr)):
            curr_sum+=arr[i]
            max_sum=max(curr_sum,max_sum)
            if(curr_sum<0):
                curr_sum=0
        return int(max_sum)
